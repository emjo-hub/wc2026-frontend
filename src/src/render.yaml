services:
  - type: web
    name: wc2026-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://wc2026-backend-delta.vercel.app
